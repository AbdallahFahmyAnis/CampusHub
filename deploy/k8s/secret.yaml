apiVersion: v1
kind: Secret
metadata:
  name: campushub-secrets
  namespace: campushub
type: Opaque
stringData:
  Identity__ClientSecret: gateway-dev-secret
  Identity__GatewayClientSecret: gateway-dev-secret
  Internal__ApiKey: campus-dev-internal
  Access__SigningKey: campus-dev-qr-signing
  # Replace before any shared cluster. These match local development defaults.
