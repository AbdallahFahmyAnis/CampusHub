@page "/ops/denied"
@model DeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<h1>Access denied</h1>
<p class="ops-lede">The operations console is limited to the Administrator role.</p>
<p class="d-flex flex-wrap gap-2">
    <a class="btn btn-primary" href="/">Back to CampusHub</a>
    <a class="btn btn-outline-primary" href="/login?returnUrl=/ops">Sign in as admin</a>
</p>
